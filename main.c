#include "stdio.h"
#include "stdlib.h"
#include "menu.h"
#include "struct.h"
#include "book.h"
int main(){
 //void menu();
 //int choice;
 Book *book;
 menu(book);
/* scanf("%d",&choice);
 if(choice==1)
    printf("2");
 if(choice==2)
    printf("3");*/
 return 0;
}

